# 🎭 Sentiment Classifier (0, 1, 2)

A Python-based machine learning tool that classifies text into **Negative**, **Neutral**, or **Positive** sentiments using **XGBoost** and **Scikit-Learn**.

---

## 🚀 Quick Start

### 1. Prerequisites
Ensure you have Python 3.13+ installed. Install the required dependencies:
```bash
pip install pandas matplotlib wordcloud xgboost scikit-learn joblib
